{% extends 'base.html.twig' %}

{% block title %}Éditer la série{% endblock %}

{% block body %}
    <div class="row d-flex justify-content-center">
        <h1 class="title-admin">Éditer la série</h1>
        <h2 class="pt-1"><a href="{{ path('program_index') }}"><i class="fa fa-arrow-left"></i></a></h2>
        <div class="d-flex align-items-center ml-3">
            {{ include('program/_delete_form.html.twig') }}
        </div>
    </div>
    <div class="col-10 offset-1">
        {{ include('program/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    </div>
{% endblock %}
