<nav class="navbar fixed-top navbar-expand-lg navbar-light pl-4 pr-4 pt-0 pb-0">
    <div class="d-flex justify-content-between navbar-content">
        <div class="row">
            <a class="navbar-brand p-0 d-flex align-items-center" href="/">
                <img src="/img/zapper-logo.png" class="d-inline-block align-top navbar-logo" alt="" loading="lazy">
                <p class="p-0 pb-1 m-0 navbar-title">apper</p>
            </a>
            <a href="{{ path('zapper_index') }}" class="nav-link d-flex align-items-center ml-4">Séries</a>
            <a href="{{ path('zapper_index') }}" class="nav-link d-flex align-items-center ml-4">Favoris</a>
        </div>
        <div class="d-flex">
            <div class="row">
                <div class="collapse navbar-collapse" id="navbarNavDropdown">
                    {% if is_granted('ROLE_ADMIN') %}
                    <ul class="navbar-nav">
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle ml-2" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                Index
                            </a>
                            <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                                <a class="dropdown-item" href="{{ path('program_index') }}">Index des séries</a>
                                <a class="dropdown-item" href="{{ path('season_index') }}">Index des saisons</a>
                                <a class="dropdown-item" href="{{ path('episode_index') }}">Index des épisodes</a>
                                <a class="dropdown-item" href="{{ path('category_index') }}">Index des catégories</a>
                                <a class="dropdown-item" href="{{ path('actor_index') }}">Index des acteur·rice·s</a>
                            </div>
                        </li>
                    </ul>
                    {% endif %}
                </div>
                {% if app.user %}
                    <div class="d-flex align-items-center">
                        <div class="user-pseudo d-flex align-items-center ml-3">{{ app.user.pseudo }}</div>
                    </div>
                    <a href="{{ path('app_logout') }}" class="nav-link d-flex align-items-center ml-2"><i class="fa fa-user-times "></i></a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="nav-link d-flex align-items-center ml-2"><i class="fa fa-user-plus"></i></a>
                {% endif %}
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
            </div>
        </div>
    </div>
</nav>
