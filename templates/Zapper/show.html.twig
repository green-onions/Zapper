{% extends 'base.html.twig' %}

{% block body %}
    {% if title != '' %}
        <h1>Titre de la série : {{ title }}</h1>
    {% else %}
        <h1>Aucune série sélectionnée, veuillez choisir une série</h1>
    {% endif %}
{% endblock %}