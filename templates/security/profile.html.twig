{% extends 'base.html.twig' %}

{% block title %}Mon profil{% endblock %}

{% block body %}
    <div class="row d-flex justify-content-center align-items-center">
        <h1>Mon profil</h1>
    </div>
    <div class="card-profile col-4 offset-4 d-flex flex-column justify-content-center align-items-center">
        <h2 class="raleway">{{ app.user.pseudo }}</h2>
        <h2 class="raleway">{{ app.user.email }}</h2>
    </div>
{% endblock %}
